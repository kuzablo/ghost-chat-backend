meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: cd frontend && npm install && npm run build && cd .. && npm install

run:
  command: node server.js
  containerPort: 3000
  persistenceMount: /data